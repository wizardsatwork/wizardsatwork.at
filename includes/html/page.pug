doctype html

html(lang='en')
  head
    meta(charset='utf-8')

    block title
      title Wizards & Witches @ Work

    link(
      rel='icon'
      href='/favicon.ico'
    )

    meta(
      name='description'
      content='Wizards & Witches @ Work - magically shifting paradigms'
    )

    meta(
      name='viewport'
      content='width=device-width, initial-scale=1'
    )

    link(
      rel='stylesheet'
      href='/main.css'
    )

  body(id='♥')
    .main
      block content
        div no content

      block footer
        include /footer.pug

    block scripts
      script.
        var d = document;
        var b = d.body;
        var s = d.createElement('script');
        b.className = b.className.replace('nojs', 'js');
        s.src = '/js/index.js';
        b.appendChild(s);
